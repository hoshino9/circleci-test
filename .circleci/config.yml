version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Compile
          command: g++ main.cpp -o main.run

      - run:
          name: Test
	  command: main.run